# -------------------------------------------------------
# 루트 .env.example — postgresql-mcp 도구 전용
# 백엔드 환경 변수는 backend/.env.example 참조
# -------------------------------------------------------

# postgresql-mcp 연결용 (개발 도구 전용)
POSTGRES_CONNECTION_STRING=postgresql://todolist_user:todolist_pass@localhost:5432/todolist_app
